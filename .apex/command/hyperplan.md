---
description: Run adversarial multi-agent planning.
agent: pathfinder
---

Create an adversarial plan for: $ARGUMENTS

Load the `hyperplan` skill immediately and run its full workflow: produce a plan, then run critical reviewers, refine, and output a final decision-complete plan under `.omo/plans/`.
